<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.vishesh.musicwiki">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:targetApi="m">
        <activity android:name="com.vishesh.musicwiki.ArtistAlbumDetailScreen.ArtistDetailsActivity" />
        <activity android:name="com.vishesh.musicwiki.ArtistAlbumDetailScreen.AlbumDetailsActivity" />
        <activity android:name="com.vishesh.musicwiki.TagsFragmentDetailsScreen.TagDetailsActivity" />
        <activity android:name="com.vishesh.musicwiki.MainScreen.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>